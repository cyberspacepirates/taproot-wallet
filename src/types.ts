export enum ScreenState {
  Receive = "receive",
  Send = "send",
  Address = "addresses",
  SilentPayment = "silent-payment",
  UTXOS = "utxos",
}

export enum AppState {
  None = "none",
  Full = "full",
  WatchOnly = "watch-only",
}
